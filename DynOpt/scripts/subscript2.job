# input parameters:
#  $1 -> predictor type
#  $2 -> algnameaddition
#  $3 -> dimension

functions=(sphere rastrigin rosenbrock)
poschgt="sinefreq"

for f in "${functions[@]}"
do
	./run_server.sh "$1" "$2" "$3" "$f" "$poschgt" &
done

wait
